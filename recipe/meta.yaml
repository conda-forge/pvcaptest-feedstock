{% set version = "0.11.1" %}

package:
  name: pvcaptest
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/c/captest/captest-{{ version }}.tar.gz
  sha256: 7086e54e34f5a34686b10fd2723f9264ab07cf514d13a5c01fac2adc3cbe0657

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - pip
    - python >=3.7
    - versioneer

  run:
    - bokeh >=1
    - matplotlib-base >=2
    - numpy >=1.13.0
    - pandas >=1
    - python >=3.7
    - python-dateutil >=2.5
    - scikit-learn >=0.19
    - statsmodels >=0.8
    - holoviews >=1.11
    - pvlib-python >=0.9
    - param
    - colorcet
    - openpyxl

test:
  imports:
    - captest
  requires:
    - pip
  commands:
    - pip check

about:
  home: http://github.com/pvcaptest/pvcaptest
  license: MIT
  license_file: ./LICENSE.txt
  summary: Framework and methods to facilitate photovoltaic facility capacity testing following ASTM E2848.
  doc_url: https://pvcaptest.readthedocs.io/en/latest/

extra:
  recipe-maintainers:
    - bt-
